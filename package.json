{
    "name": "industrial-visit",
    "version": "1.0.0",
    "private": true,
    "repository": {
        "type": "git",
        "url": "git+https://github.com/ranjith-v-code/Industrial-Visit.git"
    },
    "license": "UNLICENSED",
    "scripts": {
        "deps": "npm install && cd server && npm install && cd ../client && npm install",
        "start": "run-p start:server start:client",
        "start:server": "cd server && npm start",
        "start:client": "cd client && npm start"
    },
    "engines": {
        "node": ">= 18"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
